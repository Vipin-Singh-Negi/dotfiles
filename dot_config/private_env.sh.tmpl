{{- /*
  Tier-Low secrets ONLY: low blast radius, needed in every shell.
  High-value credentials (cloud admin, prod, signing) must NOT go here -
  use the `secret` shell function or direnv so they never touch disk.

  Rendered at apply time; mode enforced by run_after_90-secure.sh.
*/ -}}
# GENERATED BY CHEZMOI - DO NOT EDIT. Source of truth: your Bitwarden vault.

export GITHUB_TOKEN="{{ includeTemplate "secret" (dict "item" "github-token" "ctx" .) }}"
{{ if .isWork }}
export WORK_PROXY_TOKEN="{{ includeTemplate "secret" (dict "item" "work-proxy" "ctx" .) }}"
{{ end -}}
