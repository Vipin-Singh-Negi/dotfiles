# Line endings are the #1 cross-platform dotfiles bug: without this a Windows
# checkout turns every script CRLF and Unix dies with `bad interpreter: ^M`.
* text=auto eol=lf
*.ps1 text eol=crlf
*.png binary
*.jpg binary
*.pdf binary
*.age binary
