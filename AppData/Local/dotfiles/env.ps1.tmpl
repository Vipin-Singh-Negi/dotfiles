# GENERATED BY CHEZMOI - DO NOT EDIT.
# ACLs are enforced by run_after_90-secure.ps1 because chezmoi's private_
# attribute is a silent no-op on Windows.

$env:GITHUB_TOKEN = "{{ includeTemplate "secret" (dict "item" "github-token" "ctx" .) }}"
{{ if .isWork }}
$env:WORK_PROXY_TOKEN = "{{ includeTemplate "secret" (dict "item" "work-proxy" "ctx" .) }}"
{{ end -}}
